export class Movie {
    title: string;
    year: number;
    poster:string

    constructor(title: string, year: number, poster:string) {
        this.title = title;
        this.year = year;
        this.poster = poster;
    }
    
}